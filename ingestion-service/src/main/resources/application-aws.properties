# === "aws" profili: GERCEK AWS'ye baglanir ===
# Bos endpoint  -> endpointOverride yapilmaz, SDK gercek AWS adresine gider.
# Bos anahtarlar -> DefaultCredentialsProvider (~/.aws / IAM rol) kullanilir.
# Calistirma: mvnw spring-boot:run -Dspring-boot.run.profiles=aws
aws.endpoint=
aws.access-key=
aws.secret-key=
