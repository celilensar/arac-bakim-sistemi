spring.application.name=ingestion-service

# --- AWS / LocalStack ---
aws.region=eu-central-1
aws.endpoint=http://localhost:4566
aws.access-key=test
aws.secret-key=test

# --- Kaynak isimleri ---
app.sqs.queue-name=telemetry-queue
app.dynamodb.table-name=telemetry

# Kuyrugu kac milisaniyede bir yoklayacagiz (bir onceki cevrim bitince beklenecek sure)
app.poll.interval-ms=1000
