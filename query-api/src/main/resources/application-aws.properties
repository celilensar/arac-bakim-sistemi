# === "aws" profili: GERCEK AWS'ye baglanir ===
# Bos endpoint  -> endpointOverride yapilmaz, SDK gercek AWS adresine gider.
# Bos anahtarlar -> DefaultCredentialsProvider (App Runner IAM rolu / ~/.aws) kullanilir.
# App Runner'da: ortam degiskeni SPRING_PROFILES_ACTIVE=aws ile devreye alinir.
aws.endpoint=
aws.access-key=
aws.secret-key=
